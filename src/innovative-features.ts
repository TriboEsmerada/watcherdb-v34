/**
 * Innovative Features — TypeScript migration stub
 *
 * This file will gradually replace static/js/innovative_features.js
 * For now, it serves as an entry point for the Vite build.
 */

// Re-export security utils
export { safeHTML } from './security-utils';

// The actual innovative_features.js is still loaded as vanilla JS
// Migration will happen incrementally
console.log('[WatcherDB] TypeScript build pipeline active');
