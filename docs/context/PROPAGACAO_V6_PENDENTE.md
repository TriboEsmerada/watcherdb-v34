# Backlog de propagação V3.3 → V6 (pendente)

Living doc. Regras aplicáveis: `feedback_v33_v6_propagation_rule` (fixes V3.3 propagam a V6)
e `feedback_v6_portal_not_superset` (portal V6 NÃO é superset — grep anchors no V6 primeiro;
o item pode nem existir lá).

| # | Data V3.3 | Item | Onde no V3.3 | Nota p/ aplicador V6 | Estado V6 |
|---|-----------|------|--------------|----------------------|-----------|
| 1 | 2026-07-14 | Botão discreto de ciclo de tema na vista de instância (server header) — `id=serverHeaderThemeBtn`, `cycleTheme()`, ícone sincronizado via `_kpiSyncThemeBtns()` | `templates/watcherdb_portal.html` (~4420 + ~32162) | V6 tinha cycleTheme mas SEM controlo na vista de instância. Aplicado 2026-07-14: botão no server header + _kpiSyncThemeBtns estendido + listener wdb:themechange (adaptação: V6 dispara evento no applyTheme) | APLICADO e2ca5bc (aguarda browser test V6) |
| 2 | 2026-07-14 | Remoção da feature "Compacto" do dashboard KPIs (botão + `toggleKPICompactMode()` + CSS `kpi-dashboard-compact`/`kpi-compact-toggle` + chave em `SYNCED_PREF_KEYS`) | `templates/watcherdb_portal.html` (8 edits, +17/−173) | Aplicado 2026-07-14. DIVERGÊNCIA V6: classe `.kpi-compact-toggle` MANTIDA (é estilo partilhado dos botões Limpar filtro/Idioma no V6); removido só feature (botão, função, CSS kpi-dashboard-compact, localStorage read). V6 não tem SYNCED_PREF_KEYS | APLICADO e2ca5bc (aguarda browser test V6) |
| 3 | 2026-07-14 | Fix responsividade do card Resumo Executivo: grelha 5 colunas fixas transborda (números/ícones sobrepostos) com sidebar expandida (card 520-780px) | `templates/watcherdb_portal.html` CSS `.rep-exec-grid` (~1648-1652) | Aplicado no V3.3 e no V6 2026-07-14 (CSS era idêntico, fix 1:1): `repeat(auto-fit, minmax(170px,1fr))` base + `minmax(150px,1fr)` no breakpoint 520; override 330 removido | APLICADO e2ca5bc (aguarda browser test V6) |
| 4 | 2026-07-15 | Fix race: dashboard KPIs pinta por cima da tab activa (ex.: Log) — render global lento/cold-start retry (30s) resolve DEPOIS de o user abrir tabs e escreve `innerHTML` em `tabsContentArea`, destruindo os contentElements das tabs (report owner 15-07, PRD213 Overview lento → Log → dash por cima) | `templates/watcherdb_portal.html` `renderDashboardCardsContent()` (~32416-32677: writes em 32432, 32512, 32661+retry 32663, 32667) + `_scheduleStaleRefresh` `_attempt` (~32740: guard só corre se tabId set) | Guard re-check PÓS-await antes de cada write quando `tabId===null`: se `openTabs.size>0 || (selectedServer && selectedServer.server_id!=='dashboard-kpis')` → abortar sem escrever. Aplicado V3.3: helper `_globalDashRenderObsolete()` + 3 guards (topo função, pós-fetch, topo catch). Grep V6 por `renderDashboardCardsContent`/`tabsContentArea` primeiro (portal V6 não é superset) | APLICADO V6 6bed5df 2026-07-19 (helper `_globalDashRenderObsolete` + 3 guards; sub-parte `_scheduleStaleRefresh` N/A — nao existe no V6; aguarda browser test V6) |
| 5 | 2026-07-15 | Modal Backup Failed não diz QUAL database falhou — só Tipo (DIFF/FULL/LOG). API já envia `Job_Name`/`Step_Name`/`Message` (e `Database`, mas NULL nas rows `sysjobhistory` — falha é job-level) e o branch genérico backup do modal descarta-os | Frontend: `templates/watcherdb_portal.html` ~35089-35097 (branch `kpiType.includes('backup')`); Backend já OK: `api/routers/intelligence_kpis.py:1481-1496` | Quick win: renderizar Job_Name + Step + excerpt da Message no item (como o branch `jobs` faz em ~35128-35138) — Job_Name/Message tipicamente revelam a DB. Fix completo (collector V1 extrair DB da Message) liga ao deferred R+14 Silent Backup Failure | APLICADO V6 6bed5df 2026-07-19 (ADAPTADO ao padrao alarm-cards do V6: novo branch backup-failed/log-failed com chips Job/Step + linha Message; backend V6 ja enviava os campos; aguarda browser test V6) |
| 6 | 2026-07-15 | Perf: Overview de instância fria demora (handshake TCP+login era criado DENTRO do lock per-server do pool → os 7 fetches paralelos do Overview serializavam, custo de conexão ×7 em wall-clock) + pool esfriava em 5 min | `api/connection_pool.py` `get_connection()` (criação de conexão movida p/ FORA do lock; lock só p/ lista do pool) + `POOL_CONNECTION_LIFETIME` 300→1800 | Verificar se V6 usa o mesmo connection_pool.py (grep `SQLServerConnectionPool` / `POOL_CONNECTION_LIFETIME`); fix é 1:1 se o ficheiro for cópia. Cap do pool continua garantido no return_connection | APLICADO V6 6bed5df 2026-07-19 (ADAPTADO: criacao fora do lock preservando circuit breaker V6; lifetime env default 300→1800; sem venv V6 p/ pytest — validar pos-restart) |
| 7 | 2026-07-16 | UX Performance (Queries Lentas / Heavy Queries): células truncadas a 120c sem forma de copiar a query completa (hover só mostra). Célula truncada agora é clicável → popover com valor completo em `<pre>` + botão Copiar (clipboard API + fallback textarea p/ HTTP LAN) | `templates/watcherdb_portal.html`: `perfRenderStepTable`/`_perfBuildTableBody` (+tid/viewRows), `perfSortTable` (sync viewRows), novas `perfShowCellFull`/`_perfCopyText`/`_perfCopyTextFallback` (~49665-49810) | Rendering é partilhado por TODAS as tabelas L4/L5 do módulo Performance — 1 fix cobre todos os investigators. Grep V6 por `_perfBuildTableBody` primeiro (portal V6 não é superset); backend não trunca (SUBSTRING offsets DMV = statement completo) | APLICADO V6 6bed5df 2026-07-19 (ADAPTADO a `_perfRenderTableHtml` do V6 — `state.viewRows` no render + celula clicavel + `perfShowCellFull`/`_perfCopyText` no mesmo IIFE; aguarda browser test V6) |
| 8 | 2026-07-16 | Bug: acordeões (ex. "Backups com Issues") não abrem quando há 2 tabs do mesmo módulo abertas (ex. Backup da secundária + primária do AG) — secções usam IDs fixos e `toggleSection()` via getElementById apanha a 1ª ocorrência (tab escondida) | `templates/watcherdb_portal.html` `toggleSection()` (~7503): resolve `#id` dentro de `.tab-content.active` primeiro, fallback global | Fix cobre TODOS os módulos com secções colapsáveis (Backup/Jobs/Users/Maintenance…). LIMITAÇÃO conhecida (follow-up): filtros/sort internos do módulo Backup também usam IDs fixos — 2 tabs Backup simultâneas podem interferir entre si; escopar por tabId é refactor maior, não feito. Grep V6 por `toggleSection` | APLICADO V6 6bed5df 2026-07-19 (1:1; aguarda browser test V6 com 2 tabs Backup) |
| 9 | 2026-07-16 | UX loading: checklist REAL de carregamento no skeleton do Overview — cada endpoint (Serviços/CPU/Memória/Databases/Backup×3) marca ✓ + duração quando a resposta chega de facto, com relógio decorrido; user vê o que carrega e o que falta (pedido owner: "distrair lendo enquanto carrega", implementado com progresso verdadeiro, não fake) | `templates/watcherdb_portal.html`: skeleton HTML (`#skeleton-checklist`+`#skeleton-elapsed` ~6592), helpers `skelStepStart`/`skelStepDone`/ticker (~13315, scoped a content — imune a tabs duplicadas), hook em `timedFetch` (~13540) | REPLICADO A TODOS os módulos (pedido owner 2026-07-16): helper genérico `startLoadingChecklist(content, msg)` + `lc.track(label, promise)` junto ao `createPremiumLoading` (~6511); wired em Log(2), Disk(5), Backup(4), Encrypted(2), AlwaysOn(2 fases: validação + AG/eventos), SQL Diag(2), Space/Memory/CPU/Users/Jobs/Security/Services(1 cada) = 27 track sites + Overview via timedFetch. Grep V6 por `createPremiumLoading`/`createOverviewSkeleton` | APLICADO V6 6bed5df 2026-07-19 (Overview skeleton+timedFetch 7 passos + `startLoadingChecklist` wired em 14 modulos incl. AlwaysOn 2 fases e SQL Diag; ADAPTADO: strings PT hardcoded — portal V6 nao tem t(); 10/10 script blocks OK; aguarda browser test V6) |
| 10 | 2026-07-17 | Perf tab Backup: summary pedia `days=365` mas a data de "último backup" no backend é TOP 1 SEM janela (nada se perde ao reduzir); 365d só pesava na análise histórica de gaps LOG + parse TDP. Reduzido para 60d (decisão owner entre 30/60; 60 preserva padrões de FULL mensal) | `templates/watcherdb_portal.html`: `loadBackupAnalysis` summaryUrl + label checklist + modal resumo consolidado (~23871) — 2 call sites `days=365`→`days=60`. Export CSV (~19366) MANTÉM 365 deliberadamente (acção manual, não afecta load). Backend inalterado (default já era 30, `ge=1 le=365`) | Grep V6 por `summary?days=` no portal (3 sites esperados) | APLICADO V6 6bed5df 2026-07-19 (2 sites 365→60: loadBackupAnalysis + modal resumo; export CSV mantem 365; Overview ja era 30d) |
| 11 | 2026-07-17 | Bug: spinner eterno "Selecione um servidor ou aguarde os KPIs" ao fechar a última tab — `closeTab` escreve o spinner mas nunca chama `renderDashboardCards()`; dependia do auto-refresh global que foi limpo quando as tabs abriram (recupera às vezes em ~30s se o intervalo sobreviveu, senão nunca) | `templates/watcherdb_portal.html` `closeTab` branch "Nenhuma aba restante" (~6461-6481): adicionar chamada `renderDashboardCards()` após o spinner | Grep V6 por `select_server_or_wait`/`closeTab` | APLICADO V6 6bed5df 2026-07-19 (1:1 — renderDashboardCards() no branch sem-tabs do closeTab; aguarda browser test V6) |
| 12 | 2026-07-17 | Falso positivo "DBs com Problema": réplica secundária NÃO-legível de AG mostra todas as DBs do AG como "Sem acesso (is_accessible=0)" (PRD013\I03: 32/37 — role SECONDARY confirmado pelo owner via DMV). Sem acesso numa secundária não-legível é comportamento normal do AlwaysOn | Backend `api/routers/queries/space.py` get_databases: +coluna `is_ag_secondary` (joins dm_hadr_database_replica_states + dm_hadr_availability_replica_states, is_local=1 role=2). Frontend `watcherdb_portal.html`: loop attention exclui is_ag_secondary=1 da contagem (contador à parte) + nota informativa azul no modal showDbProblemsModal | Requer Restart-Service (space.py é backend). Query via sql_monitoring, read-only, VIEW SERVER STATE já granted; non-AG → NULL→0. Cache _DATABASES_CACHE serve payload antigo até TTL. Grep V6 por `is_accessible`/`showDbProblemsModal` | PARCIAL V6 6bed5df 2026-07-19 (backend sql_queries.py get_databases +is_ag_secondary FEITO; frontend N/A — o loop attention do V6 nunca contou is_accessible=0 como problema, o falso positivo nao existe la; requer restart servico V6) |
| 13 | 2026-07-17 | Pacote AlwaysOn-em-secundária (4 fixes, decisão owner A+B+C+D): (A) descoberta da primária falhava em secundárias — query usava DMV de réplica (`role_desc='PRIMARY'` só resolve na primária); trocada para `dm_hadr_availability_group_states.primary_replica` (metadados WSFC, funciona em qualquer réplica; mesmo padrão do módulo Backup) + fallback legacy. (B) Nome da primária no banner é link que abre Overview dela (novo helper `openTabForServerName`, normalize robusto a `\` comido no onclick). (C) Relatório: regra AG002 tratava estado por-DB VAZIO (normal em secundária) como "não sincronizada" → CRITICAL falso contradizendo card HEALTHY; vazio já não conta + nova regra AG005 (WARNING honesto "relatório gerado em secundária, gerar na primária p/ dados completos"). (D) Card Listener do relatório: overflow de string longa (font 15px + word-break) | (A) `modules/monitoring/watcherdb_alwayson_check.py` ~380; (B) `watcherdb_portal.html` helper pós-navigateToPrimaryBackup + banner ~28600; (C) ALWAYSON_RULES AG002+AG005 ~48160; (D) report-kpi-card listener ~48240 | A é backend → Restart-Service. Grep V6 por `ALWAYSON_RULES`/`fetchFromPrimaryNode`/`primary_query` | PARCIAL V6 6bed5df 2026-07-19 (A primary_replica WSFC + fallback FEITO em watcherdb_alwayson_check.py; B link banner + openTabForServerName FEITO; C+D N/A — V6 nao tem ALWAYSON_RULES nem relatorio AG com regras; requer restart servico V6) |
| 14 | 2026-07-17 | Bug: botão "Testar" das Queries Customizadas (Configurações) rebentava com "openDynamicTab is not defined" — chamada a função do sistema de tabs ANTIGO que nunca existiu no portal actual; o resultado do teste nunca abria | `templates/watcherdb_portal.html` window.testCustomQuery (~42304): substituída tab fantasma por modal próprio `customQueryTestModal` (header azul + SQL + tabela de resultados + total de linhas) | Grep V6 por `openDynamicTab`/`testCustomQuery` — se V6 tiver o mesmo resquício, mesmo fix | APLICADO V6 6bed5df 2026-07-19 (mesmo resquicio openDynamicTab existia; modal customQueryTestModal, padrao .modal.active confirmado; aguarda browser test V6) |
| 15 | 2026-07-17 | UX Space: cards "Tamanho Atual"/"Uso Atual" do relatório de Análise Preditiva de Crescimento mostravam MB com 2 decimais (40724.48 MB) — pedido owner: sem decimais | `scripts/filegroup_interactive_report_v5_watcherdb.py` ~516-521 (`:.2f`→`:.0f`; % mantém 1 decimal). Script corre por subprocess a cada geração — sem restart, basta gerar novo relatório. EXTRA 2026-07-17: `generateReport` no portal ganhou `timeout: 300000` — o default 60s do createFetchWithAbort matava relatórios lentos a meio ("Failed to fetch") apesar de o loading avisar 30-90s | Grep V6 por `filegroup_interactive_report` (verificar qual variante o V6 invoca) + `generate-report` no portal (mesmo timeout default provável) | PARCIAL V6 6bed5df 2026-07-19 (timeout 300000 no generateReport FEITO; `:.0f` N/A — nenhum filegroup_interactive_report*.py existe no repo V6, o PredictiveAnalyzer desactiva a feature por script ausente) |
| 16 | 2026-07-17 | FEATURE NOVA: tab "Sessions" por instância (gap competitivo identificado pelo owner+Ricardo — Watcher não respondia "quem está ligado e a executar o quê AGORA"). Equivalente ao painel Processes do Activity Monitor: 16 colunas AM (Session ID/User Process/Login/Database/Task State/Command/Application/Wait Time/Wait Type/Wait Resource/Blocked By/Head Blocker/Memory/Host/Workload Group/CPU) + filtros por coluna AM-style ((Todos)/(Vazias)/(Não vazias)/valores), sort por cabeçalho, chips resumo (total/activas/user/bloqueadas/head blockers), destaque visual blocking, coluna Query com clique-para-copiar (popover), botão "copiar KILL" (SÓ copia — portal continua read-only), auto-refresh 10s opcional preservando filtros, secção Sessões Problemáticas (endpoint SQL Diag existente) em `<details>` | Backend: `api/routers/queries/performance.py` novo endpoint `/api/queries/active-sessions/{server_id}` (ACTIVE_SESSIONS_QUERY: dm_exec_sessions+requests+sql_text+resource_governor; sql_monitoring VIEW SERVER STATE). Frontend: `watcherdb_portal.html` nav-btn sessions + getTabLabel + 2 dispatches + SESSIONS MODULE completo (antes do DISK MODULE); i18n `tab.sessions` nos 3 locales | Requer Restart-Service V33 (endpoint novo) + Ctrl+Shift+R. Tier: Std (core monitoring) — passar v33-feature-matrix-checker pré-commit. Grep V6 por `active-sessions`/`SESSIONS_COLS`. ITERAÇÃO 2 (feedback owner pós-browser-test): (a) abre por defeito com filtro Task State=running; (b) interacção cruzada — session_id clicável nas DUAS tabelas destaca a sessão em ambas + scroll até à contraparte (abre `<details>` se preciso, toast se sem correspondência); (c) chips → `stat-card`/`stats-grid` padrão das outras abas, CLICÁVEIS (Total/Activa/User/Bloqueadas/Head filtram a tabela, toggle com anel azul); (d) colunas de métrica contínua (Wait Time/Memory/CPU) SEM dropdown de filtro — só ordenação (flag `nf` em SESSIONS_COLS). ITERAÇÃO 3 (feedback owner): (e) cards = vistas rápidas — clicar SUBSTITUI os filtros de coluna em vez de combinar (clicar "Total" mostra mesmo todas, não só running); (f) i18n completo da tab — grupo `sessions.*` (16 chaves) em PT/EN/ES, zero mistura de idiomas nos cards/controlos (regra do translate); (g) auto-refresh LIGADO por defeito a 30s com selector 5/10/30/60s (`sessionsSetAutoSecs`, timer reinicia ao mudar). ITERAÇÃO 4 (feedback owner): (h) card Running (subset de Tarefa Activa, RUNNING puro) + botões "?" em TODOS os 6 cards via padrão CARD_HELP_TEXTS/showCardHelp existente (6 entradas novas `sess-*` com explicação DBA-grade: RUNNING vs RUNNABLE vs SUSPENDED, head blocker, etc.); (i) FIX copy: `_perfCopyText` vive num IIFE noutro script block — KILL/copiar query falhavam com ReferenceError silencioso; helpers locais `_sessCopyText`/`_sessCopyFallback` no módulo; (j) sql_text backend 4000→100000 chars (queries grandes truncavam ao copiar); (k) célula Command também abre o popover da query; (l) sort: 1º clique DESC em colunas numéricas (ASC punha zeros no topo = "parece que não ordena"); (m) ordem inteligente de colunas nas 2 tabelas (identidade→estado→origem→waits+blocking→métricas→metadados; problemáticas com _PROB_PREF + desconhecidas no fim); (n) ícone ↕ das colunas de métrica é BOTÃO de ordenação (antes decorativo — user clicava sem efeito) com estado visual ▲/▼ azul; (o) tabela de Sessões Problemáticas ganha o MESMO padrão da principal: 8 colunas de métrica (`_SESS_PROB_NUMERIC`: wait_time, cpu_time, memory_usage_kb, total_elapsed/scheduled_time, reads, writes, logical_reads) com sort-only DESC-first, restantes com dropdown de filtro AM-style, contador "X de Y", estado próprio (probFilters/probSortCol). NOTA V6: verificar scoping de copy-helpers (IIFE!) e assinatura do t() (v2: flatten + {param}). (p) card "Pessoas (Interactivo)" — sessões de utilizador abertas por ferramentas interactivas (`_SESS_HUMAN_TOOLS`: SSMS/ADS/sqlcmd/DBeaver/DataGrip/HeidiSQL/Toad, heurística por Application — critério explicado no "?"); posição do botão Sessions trocada com SQL Diag (Sessions no lugar do SQL Diag, este vai para o fim da fila 2) | APLICADO V6 6bed5df 2026-07-19 (endpoint active-sessions em sql_queries.py + modulo completo com iteracoes 1-4 incl. card Running/Pessoas + copy helpers locais; nav: Sessions no lugar do SQL Diag, SQL Diag p/ fim da fila 2; ADAPTADO: strings PT hardcoded + help "?" estilo inline V6 — i18n N/A; requer restart servico V6 + browser test) |
| 17 | 2026-07-17 | Documentação dos KPIs (modal "?") desactualizada face à realidade: (a) entrada Mirroring reescrita — estados problema completos (DISCONNECTED/SUSPENDED/SYNCHRONIZING/PENDING_FAILOVER + principal não-ONLINE), tabelas reais (STG/ACTIVE/DET_VIEW em vez de "mirroring_status"), cadências 5/5/10, nota sobre SYNCHRONIZING prolongado + redo queue (lição do incidente PRD301/302); (b) Deadlocks — removida claim falsa "zero pegada" + cadências antigas (o parse XML foi exactamente o motivo do disable); (c) Blocked Users marcado LEGADO (substituído por Blocked Sessions) com ponteiro para a tab Sessions. PT+EN+ES nas 3 entradas (excepto howItWorks EN/ES do Blocked Users — drift menor) | `templates/watcherdb_portal.html` KPI_DOCUMENTATION (~36630 deadlocks, ~36440 blocked-users, ~37470 mirroring) | Grep V6 por `KPI_DOCUMENTATION` — verificar se V6 tem o mesmo modal e sincronizar texto | APLICADO V6 6bed5df 2026-07-19 (ADAPTADO PT-only — KPI_DOCUMENTATION V6 nao tem sub-objectos i18n; entradas mirroring ('mirroring') + deadlocks + blocked-users sincronizadas) |
| 18 | 2026-07-17 | Bug UX: caixa "Buscar servidor" disparava o gestor de passwords do Chrome (oferecia credenciais guardadas em vez das sugestões de instâncias) — Chrome ignora `autocomplete="off"` em `type=text` sem `name` quando há credenciais do site | `templates/watcherdb_portal.html` `#searchInput` (~3712): `type="search"` + `name="watcherdb-server-filter"` + `role="searchbox"` + CSS `::-webkit-search-cancel-button` escondido (X próprio já existe). PLANO B (type=search não chegou no Chrome do owner): `readonly`-até-ao-foco — campo nasce readonly (password manager nunca oferece), destrava 60ms após focus, re-trava no blur (cada clique começa readonly). EXTRA: Enter no autocomplete abre a PRIMEIRA sugestão (antes exigia navegação por setas — `_acIndex >= 0` — e não fazia nada) | Grep V6 por `searchInput`/`search-input`/`_acIndex` — V6 tem login + caixa de busca igual, mesmos 2 bugs prováveis | APLICADO V6 6bed5df 2026-07-19 (type=search + name + readonly-ate-foco + CSS webkit; Enter ADAPTADO a acSelectItem(indice) do V6 — abre 1a sugestao sem setas; aguarda browser test V6 no Chrome do owner) |
| 19 | 2026-07-17 | D2+D4: Deadlocks two-tier counter-gated (V1, partilhado — V6 beneficia automático) + **Freshness Guard Componente A**: view `KPI_MSSQL_COLLECTION_FRESHNESS_VIEW` na Intelligence (partilhada) + lado V3.3 a propagar: `collect_collection_freshness()` em helpers.py + chamada na cadeia do dashboard (intelligence_kpis.py) + badge **⚠ STALE âmbar** em `_renderKpiCard` (substitui OK quando `data_stale`, tooltip com minutos). Review v1-intel-specialist PASS (3 correcções aplicadas: heartbeat warning, GRANT UPDATE, errorlog→HEARTBEAT). DDL corrigido pós-execução owner: coluna real é `Last_Swap_Time` (não Updated_At) | V3.3: `helpers.py` final (collect_collection_freshness), `intelligence_kpis.py` (import+chamada pós-Phase3), `watcherdb_portal.html` `_renderKpiCard` (~32605). V6: grep `_renderKpiCard`/`format_dashboard_response` — dashboard V6 pode ter estrutura própria; a view partilhada já existe, só falta consumir + badge | V1 DEPLOY FEITO (DDL executado, validação 3 rows OK); V3.3 APLICADO 5173231; V6 APLICADO 6bed5df 2026-07-19 (ADAPTADO: collect_collection_freshness inline no intelligence_kpis.py monolitico, chamada pre-cache do dashboard; badge STALE no generateKPICards via kpi.key — view partilhada ja existe; requer restart servico V6) |
| 20 | 2026-07-19 | **WAVE X — KPI Integridade** (detecção de corrupção; spec = script do owner DIAG_ERROR_824_SWEEP.sql): view partilhada `KPI_MSSQL_INTEGRITY_VERDICT_VIEW` na Intelligence (v3: P1 suspect pages / P3 nunca validado incl. data-sentinela 0001-01-01 / P4 CHECKDB >30d / higiene PAGE_VERIFY + proxy no_checksum `_LTE7d`; **P2 823/824/825 CORTADO** — 3 bloqueios estruturais no collector errorlog, fast-follow desenhado: parse regex Error_Number + tabela estreita KPI_MSSQL_ERRORLOG_IO_EVENTS_HIST). Parecer v1-intel-specialist PASS-condicional aplicado. 1º raio-X da frota: P1=3 (DBA_RESOURCE_DB @PRD013 com 306k erros ACTIVO), P4=78 (CHECKDBs de 2019, PAGE_VERIFY NONE/TORN_PAGE). Lado V3.3 em curso (fase 4): card + modal + docs + freshness seed | V1: `database/WAVE_X_INTEGRITY_VERDICT_VIEW.sql` (+ SECAO no canonical no fecho da wave). V3.3 (fase 4, em curso): helpers `collect_integrity`, `intelligence_kpis.py` drill-down `integrity`, portal KPI_METADATA/modal/KPI_DOCUMENTATION. V6: view partilhada JÁ beneficia; propagar só o lado consumidor (card+modal+docs) — grep `KPI_METADATA`/`showProblematicInstances`. Armadilha documentada: data-sentinela 0001-01-01 do DBCC_HISTORY | V3.3 SHIPPED (branch wave-X-integridade, CHANGELOG [2.11.0], tier PASS, smoke browser OK); view v3 + seeds DEPLOYED na Intelligence (SECAO 16 canonical); V6 APLICADO cf5947e 2026-07-19 (branch wave-X-integridade no repo V6; backend + card + modal report&prescribe + nytLocalize EN/ES + freshness OR-combine + layout 4+4; aguarda browser test V6) |

Processo ao propagar: validar no V3.3 primeiro (browser test), grep anchor no V6, aplicar
adaptado, marcar Estado V6 = FEITO (commit) ou N/A (feature não existe no portal V6).

## Lote 2026-07-27 — estado por item (V6 commit `364b06e`, branch `wave-propagacao-v33-20260727`)

Spec: `PROMPT_PROPAGACAO_V6_LOTE_2026-07-27.md`. Validação: 4× py ast + 10/10 script blocks.

| Item | Estado V6 | Nota |
|---|---|---|
| A. Herança (auto-resolve, migração 009, seeds freshness, SS301, DRY_RUN) | ✅ VERIFICADO | V6 chama `usp_ResolveServerOfflineEvent` com 1 param; a 009 tem default em `@Resolved_By` ⇒ backward-compatible, nada a fazer |
| B1. FileGroups Opção A (4 sites) | ✅ FEITO | `Growth_Type='UNLIMITED'` excluído no aggregate (~1206), detail (~2587), drill (~2968) e `_ops_adapter_filegroup` (~4116) — o CASE maxsize-aware era morto (collector grava clone) |
| B1b. Satélites | ✅ PARCIAL | `morning_briefing.py` FEITO (mesmo CASE morto); catálogo Q014 +Pitfall 13 (nota, sem mexer nas rules); **`ai_assistant_service.py` ADIADO** — ficheiro dirty com +236/−22 de trabalho não commitado de outra sessão |
| B2. Fase 2 disk-bound | ✅ FEITO | Ramo `DATAFILES_STG` (Is_Unlimited + Volume_Free_MB; ≤5GB crit / ≤10GB aviso) no aggregate, drill e detail (UNION com rótulo por drive). Semântica **0 = AVISO "sem visibilidade"**, nunca crítico |
| B3. Deadlocks com nomes | ✅ FEITO | Drill +`Databases_List`/`Objects_List` (STUFF/FOR XML, piso 2014) + **branch dedicado** no modal V6 (que só tinha dump genérico): nomes truncados c/ title, contagem só quando >1 |
| B4. Resolved_By | ✅ ADAPTADO | Os 2 endpoints gravam `'portal-v6'` + fallback deploy-order-safe. **Divergência**: o V6 não tem `_require_admin` nestes endpoints (o V3.3 grava o username do admin) ⇒ adicionar o gate = decisão do owner, fora deste lote |
| B5. Collector Health HEARTBEAT | ✅ FEITO | `_fetch_schedule_meta()` + bloco no `compute_health` (usa `Expected_Interval_Minutes` como cadência real). Nota: `interval_min` local espelha o quirk do V3.3 (o JSON mostra o tick do config) |
| C1. Remover KPI "SQL Não Respondeu" | ⏸️ DECISÃO OWNER | O nome não existe no V6, mas existe o equivalente: card `server-offline` = "SQL Services Down" (`servers_sql_down`) — mesmo problema conceptual. Remover = decisão de produto, não propagação |
| C2. Painel "Eventos do collector" | ➖ N/A | O V6 já tem superfície própria (`serverOfflineModal` com summary + history 7d + Resolver, ~25850-26048). A mudança de local não se aplica; incrementos (">24h antigo", instância clicável) ficam disponíveis se o owner quiser |
| C3. Aba Log interativa | ✅ PARCIAL | Fix backend FEITO: entry `'memory'` no `category_map` do `logs_collector.py` do V6 (era código morto igual ao V3.3). **UI interativa (chips/filtros/toggles) = lote próprio** — ~200 linhas, merece GO + browser test dedicados |
| C4. Modal UX FileGroups | ✅ PARCIAL | FEITO: barra na cor da severidade do modal + Total com "(filegroups)". Já existiam no V6: card conta filegroups (F4) e `navigateToSpaceFilegroup`. Badge por contexto = já satisfeito (o caminho genérico filtra Critical/Warning por contexto). **Accordion F2 com nomes = lote próprio** |
| C5. Docs KPIs | ✅ FEITO | `backup-failed` realinhado à linguagem de operador + 3 bullets de Integridade (2571 / P4-aviso / Tipo de Falha). Entries `backup-log-failed`/`backup-no-checksum` já existiam no V6 |
| C6. "Nome, não número" | ✅ PARCIAL | Caso deadlocks FEITO (B3). Audit das restantes modais = pendente em ambos os produtos |

**Pendente do owner**: restart do serviço V6 + browser test (FileGroups sem ruído UNLIMITED, drives disk-bound, modal deadlocks com nomes, docs KPIs); decisão C1; decisão sobre `_require_admin` no V6.

## Lote 2026-07-28 — estado por item (mesma branch `wave-propagacao-v33-20260727`)

Spec: `PROMPT_PROPAGACAO_V6_LOTE_2026-07-28.md` (origem V3.3 f0cdd79 + 92c6c60).
Commits V6: `25caada` (28-A) · `d7924d6` (28-B f1) · `95d4b86` (28-B f2) · `56e7a75` (28-C).
Validação: 3× py ast + 10/10 script blocks em cada lote.

| Item | Estado V6 | Nota |
|---|---|---|
| 1(f) Backend Space | ✅ ADAPTADO `d7924d6` | Query de ficheiros reescrita p/ `USE [db]` + `sys.database_files` + `FILEPROPERTY` (UsedSizeMB) + `GrowthRaw`/`IsPercentGrowth`; `query_ag_secondary` mantida **sem USE** (fica sem Used) mas ganha growth raw; "query precisa" +`GrowthEnabledFiles`/`UnlimitedGrowableFiles`/`EffCapGB`/`EffCapMB`; mappings novos |
| 1(a) Sort par-aware | ✅ FEITO `95d4b86` | `sortNestedTable` global + **21 cabeçalhos** nas 3 tabelas (4 Databases + 8 FileGroups + 9 Datafiles); containers `fg-row-*`/`files-row-*` viajam com a linha-pai; CSS ⇅ + `lc-active-sort` |
| 1(b) Free MB/% efetivo | ✅ FEITO `95d4b86` | Matriz growth completa + reclassify (display = veredicto) + tooltip `*` no caso disk-bound; fallback à lógica antiga sem eff data |
| 1(c) FULL/MAXED | ✅ FEITO `95d4b86` | FULL (≥99,5% do current sem poder crescer) / MAXED (no maxsize com espaço interno); OVERFLOW mantém precedência |
| 1(d) Coluna Used MB | ✅ FEITO `95d4b86` | "—" em AG secondary; `colspan` da linha de datafiles 8→9 |
| 1(e) Logical Name | ✅ FEITO `95d4b86` | Sem truncagem a 35c; `min-width: 420px` + `word-break` |
| 2 Cache Space TTL 120s | ✅ FEITO `25caada` | `_SPACE_ANALYSIS_CACHE` + `cached:true` no payload |
| 2 BATCH_SIZE 5→12 | ➖ N/A | O `analyze_server_space` do V6 é **sequencial** (sem batching). Introduzir paralelismo = optimização nova + pool V6 tem 20 ligações (V3.3 tem 30) — decisão do owner, não propagação |
| 3 Fix checklist (bug-raiz) | ✅ FEITO `25caada` | `_lcInstanceSeq` + keys `lc-<inst>-<n>` + `data-lc-inst`; abort **remove** o item em vez de ✗ falso. O bug existia idêntico no V6 (helper portado a 19/07) |
| 4 Deep-link budgets | ✅ FEITO `25caada` | 15s→60s (db) e 10s→20s (fg). **Nota**: o scroll-retry 400ms/1.5s/3.2s + flash NÃO foi incluído (fora do âmbito do item 4; disponível se quiseres) |
| 5 Portal extras | ✅ FEITO `56e7a75` | Label `Full/Diff Falhou`; rodapés `_advFoot` removidos (Erros log + PAGE_VERIFY/DBs avaliadas); seletor de janela 6h/24h/3d/7d nos logs de serviço (visível também no estado vazio). Backend V6 já aceitava `hours≤168` |
| 6 SQL Browser timeout | ✅ FEITO `25caada` | Default 3.0→15.0s |
| 7 Fase B resiliência (pool) | ⛔ NÃO PORTADO | Dupla razão: (i) condição 5 do gate — `auth_scheme=KERBEROS` **pendente no próprio V3.3**; (ii) o `connection_pool` do V6 divergiu (570+/457−) e não tem `_build_server_target` — seria adaptação profunda. Reavaliar após validação Kerberos |

## Lote 2026-07-29/30 (backups e agendamentos) — estado por item

Spec: `PROMPT_PROPAGACAO_V6_BACKUPS_E_AGENDAMENTOS.md` (origem V3.3 `7e9c0fd`, `50b89e9`, `ed1f737`).
Commit V6: `3f5ebb3`. Validação: `ast` + 10/10 script blocks (baseline V6 — o prompt indica 12/12, que é a do V3.3).

| Item | Estado V6 | Nota |
|---|---|---|
| §0/§1 Herança dos 2 fixes de collector | ✅ HERDA, verificado | O V6 **não tem** cópia de `collect_agent_jobs.py` nem de `collect_backup_jobs_disabled.py` ⇒ recebe os dados corrigidos ao reiniciar o collector V1 |
| §1.2 Consumidor da semântica antiga de `HasSchedule` | ✅ **NÃO HÁ** (responde ao "por verificar no V6") | Único uso em código: `services/tool_orchestrator.py:874`, devolve a coluna num tool-result sem a interpretar. `NoScheduleJobs` (`database/KPI_AGENT_JOBS_TABLES.sql:107`) *depende* da semântica mas **não é consumido em lado nenhum**. Catálogo da AI tem `"description": "HasSchedule"` — placeholder, sem semântica errada |
| §2.1 Linha "Backup danificado" só quando > 0 | ✅ FEITO | Âncora `is_damaged_count` **existia** (veio do lote de 28/07, commit `65a81e3`). Fundamento no comentário: `is_damaged=1` exige `WITH CHECKSUM` **e** `CONTINUE_AFTER_ERROR` (intenções opostas) ⇒ o zero significa "nada apanhado onde a detecção se aplica". Critério geral registado |
| §2.2 Docs `backup-no-checksum` + `backup-failed` | ✅ ADAPTADO | `howItWorks` do no-checksum deixa de afirmar que os danificados entram; `backup-failed` passa a distinguir os dois casos. **i18n N/A**: as entradas do V6 são **PT-only** (`i18n: {` = 0 ocorrências) — o aviso dos 3 idiomas do prompt não se aplica |
| §2.3 Docs `backup-jobs-disabled` | ✅ FEITO | `description` + `howItWorks` + `thresholds` passam a dizer "enabled=0 **ou** activos com todos os agendamentos desligados". **Título intacto** — mesma decisão do V3.3 (renomear é decisão de produto em aberto) |
| §3 `Avg Livre` (FIND-20260729-106) | ✅ REMOVIDO | **Achado**: no V6 o defeito era **latente** — `AVG(Percent_Free)` era calculado e devolvido no payload mas o portal **nunca o mostrava** (o V6 já nomeia o pior drive desde o Sprint alarm-cards). Removido na mesma: peso morto e número enganador ao alcance de qualquer consumidor, incluindo a AI. Fundamento registado no código |

**Pendente do owner**: restart do collector V1 (para os dados corrigidos) + restart do serviço V6 + browser test do card Backups (a linha "Backup danificado" deve desaparecer quando `is_damaged_count = 0`). Nota: o KPI `backup-jobs-disabled` **passa a contar mais** — é o número a ficar correcto, não regressão; quem comparar séries vai ver um degrau na data do deploy.

## Wave A (2026-07-28/29) — estado por item (mesma branch `wave-propagacao-v33-20260727`)

Spec: `PROMPT_PROPAGACAO_V6_WAVE_A_2026-07-28.md` (origem V3.3 `b73aaa8` + `aa686d5` + fase B `92c6c60`).
Commits V6: `65a81e3` (A-1) · `27836f0` (A-2) · `068e8c7` (A-3).

| Item | Estado V6 | Nota |
|---|---|---|
| Secção 0 — contrato dos dados | ✅ LIDO, sem código | As 2 armadilhas (várias portas TSQL por instância ⇒ usar `CONNECTIONPROPERTY('local_tcp_port')`, não a DMV; overflow com sinal ⇒ +65536 se negativo) aplicam-se a **quem for buscar a porta ao SQL Server outra vez** — a coluna `Tcp_Port` já vem corrigida. Nenhum consumidor novo do V6 vai à DMV |
| A — herança BD | ⏳ verificação do owner | 3 queries na secção A do prompt; V6 lê a mesma BD, zero código |
| B1 fase B (IPv4+ServerSPN) | ✅ ADAPTADO `27836f0` | O pool do V6 **não tinha nenhum** dos 8 anchors (divergiu do V3.3) ⇒ construído de raiz: `resolve_ipv4_cached` (daemon thread 2s + semáforo 4 + AI_CANONNAME, **sem** getfqdn/PTR e **sem** ThreadPoolExecutor) + `_build_server_target_ex`. Condição 5 do gate (KERBEROS) **já validada no V3.3** a 28/07. Rollback: `IPV4_DIRECT_DEFAULT_INSTANCES=False`. ADAPTAÇÃO: o pool V6 não lê `servers.json` ⇒ deriva do `server_id` (ramo de fallback do V3.3); `server_id` com `\` tratado como nomeado |
| B1 net cache | ✅ FEITO `068e8c7` | `_load_net_cache` (bloco, TTL 5 min, timestamp marcado ANTES da query = sem stampede) + `_learned_target` (só devolve alvo por IP quando TEM porta) + precedência: porta aprendida → Browser → fase B. **N/A**: o passo "porta manual do `servers.json`" do V3.3 — este pool não lê o config |
| B2 monitoring.py (R7) | ✅ ADAPTADO `068e8c7` | `server_target`/`server_spn` no `ConnectionInfo` + precedência no `get_connection_string` + delegação por import tardio. **Achado que mudou a prioridade**: a prova do owner (`dm_exec_connections`, 29/07) mostrou que o caminho real do V6 é `sql_monitoring` com **`auth_scheme='SQL'`** ⇒ este era o item de maior valor, não o pool. ADAPTAÇÃO: `ServerSPN` **só no ramo Windows auth** — SQL Auth não usa SSPI; o ramo UID/PWD beneficia só do alvo IP/porta |
| B3 contagens de backup | ✅ ADAPTADO `65a81e3` | Dedupe próprio para `is_damaged`; `full/diff/other_failed_count` isolados; `failed_count` = soma (retrocompat); contadores também no fail-safe. Adaptado ao ficheiro **monolítico** do V6 (não tem `intelligence/helpers.py`). ⚠️ `no_checksum_count` **desce** — não é quebra de coleta |
| C tokens de tema DATAFILES | ✅ FEITO `65a81e3` | 11× `#000` → tokens; `files-row-` também |
| C alturas em vh | ✅ FEITO `65a81e3` | Pai 400px→70vh, filho →60vh via `.space-datafiles-scroll` |
| C botão ecrã inteiro | ✅ ADAPTADO `65a81e3` | `toggleDatafilesFullscreen` **move** o painel para overlay (preserva sort/scroll), Esc + clique no fundo. i18n: strings PT directas (V6 usa nytLocalize, não chaves `t()`) |
| C `_kpiNoiseFields` | ➖ N/A | O V6 **não tem** os dois renderizadores (`_buildCardExpandHtml`/`skipKeys` = 0) — o accordion das modais é o lote adiado. Sem os dois, não há divergência para centralizar |
| C badges `color: #fff` | ✅ FEITO `65a81e3` | 5 sítios |
| C card Backups | ✅ FEITO `65a81e3` | Full/Diff/Outros + Backup danificado; "Sem checksum" fora do card **e** do `warn` de `KPI_REPORT_GROUPS` (a armadilha) |
| E — pendentes a NÃO propagar | ✅ RESPEITADO | "Backup danificado" ficou **sem drill** (falta endpoint por `Failure_Source`); checksum no tab Backups não foi enquadrado; collector V1 a consumir a porta continua fora |

**Achado registado (não introduzido por esta wave)**: o `_build_connection_string` do `connection_pool.py` do V6 usa `Trusted_Connection=yes`, mas o caminho em uso (`monitoring.py`) usa SQL Auth com `sql_monitoring` — Regra de Ouro #2 respeitada no caminho quente. Correcção a uma afirmação minha anterior.

**Pendente do owner (Wave A)**: restart do serviço V6 + abrir uma instância **default** e uma **nomeada** no portal (valida o fail-open, que é o risco real); as 3 queries da secção A depois do DDL; prova de Kerberos só faz sentido numa instância **default** e no ramo Windows auth.

**Pendente do owner (lote 28/07)**: restart do serviço V6 + browser test da aba Space (Free efetivo, FULL/MAXED, Used MB, sort nas 3 tabelas, checklist sem ✗ falso, deep-link a frio) — caso de referência V3.3: `SQLIDSPRD03`/`DW_ODS_TAP`/`FG_BKDREV`. Se algo vier torto, verificar primeiro se `used_size_mb`/`eff_cap_mb` chegam no payload (o teste do backend da fase 1 ficou por fazer).

## Lote 2026-07-31 (tarde) — APLICADO (falta browser test + restart)

Spec completa (com Contrato dos Dados §0, obrigatorio ler primeiro):
`PROMPT_PROPAGACAO_V6_LOTE_2026-07-31_TARDE.md`. Origem V3.3: commits
`f57acb5`/`1fb09d8`/`6b06f90`/`db195f2`/`53852d1` + V1 `9a39aaf`.
Commits V6 (branch `wave-propagacao-v33-20260727`): `f22ce26` (parte 1),
`1156168` (parte 2 = P2+P3+P7), `814c384` (P6, commit isolado).

| Item | Tipo | Estado V6 |
|---|---|---|
| BD partilhada: views _ACTIVE regeneradas (slot por ambiente) + gerador corrigido | Herda via BD; sync de copias locais | ✅ FEITO `f22ce26` — o canonical do V6 tinha o CTE antigo (`CROSS JOIN ActiveSlot`), que pos-Wave C multiplica os dados ×3. Reescrito no padrao por-ambiente: `CROSS JOIN ActiveSlot` = 0, `Environment IN` = 6 |
| BD partilhada: `BACKUP_STATUS_ACTIVE` removida | Limpar referencias | ➖ N/A — 0 ocorrencias em `api/` e `modules/` do V6. Nada a limpar |
| Backend 2.1: parse ISO no caminho standalone do backup summary | Aplicar (restart) | ✅ FEITO `f22ce26` — helper `_parse_dt` ao **nivel do modulo** (o `parse_backup_date` do V3.3 e' funcao aninhada, inacessivel do caminho standalone). Aplicado a 5 campos |
| Backend 2.2: excluir DBs inacessiveis em AG (500 PRD213) | Aplicar (restart) | ✅ FEITO `f22ce26` + **correccao** (03/08) — **o V6 nao tem `FILE_SPACE_DETAIL`**; o batch dinamico equivalente e' o `LOG_SPACE_USAGE` (endpoint `/api/queries/log-space/{id}`). ⚠️ A guarda do V3.3 (`DATABASEPROPERTYEX(name,'Collation') IS NOT NULL`) **NAO funciona**: teste ao vivo no `SQLHDSPRD213_I01` devolvia 500/erro 976 na `MAP_SampleDB` na mesma — a collation vem da metadata do master e e' devolvida para secundarias nao-legiveis. Guarda efectiva = `NOT EXISTS` sobre `dm_hadr_database_replica_states` (`is_local=1 AND ISNULL(is_primary_replica,1)=0`), a mesma que o `LOG_SPACE_USAGE` do V3.3 ja usava |
| P1 Seccao Ultimos Backups por Database (aba Backup) | Portal — **feature nova** | ⏸️ NAO APLICADO — decisao do owner (anchor `last-backups-section` = 0 no V6; e' feature, nao correccao) |
| P2 Instances OK data-env + inferencia | Portal | ✅ FEITO `1156168` — inferencia espelhada no padrao **local** do V6 (card `Instances Off`, com ramo DEV), nao no do V3.3 |
| P3 Tiles Full/Diff/Outros: pre-filtro por dados + titulos + empty-state | Portal | ✅ FEITO `1156168` — `_advRow` le 7o arg via `arguments` (assinatura intacta); `showProblematicInstances(..., preFilterType)` filtra os DADOS. Verificado que `'OTHER'` **e' valor real** de `Backup_Type_Classified` (`_classify_backup_type` devolve FULL/DIFF/LOG/OTHER) — o filtro casa |
| P4 Shrink doctrine: TRUNCATEONLY + margem 30pct + notas na analise | Portal | ➖ N/A — `genResizeSQL`/`genShrinkSQL`/`openDriveDetailModal` = 0 no V6. O gerador de T-SQL nao existe la, logo nao ha risco de emitir SQL invalido |
| P5 Catalogo eventos Windows (win_events_catalog.js + hook) | Portal — **feature nova** | ⏸️ NAO APLICADO — decisao do owner (`winEventCatalogLookup`/`renderWinRows` = 0 no V6) |
| P6 Space: headers datafiles empilhados (top medido em runtime) | Portal | ✅ FEITO `814c384` — o V6 estava no estado **exacto** pre-fix (scroll proprio + `top:0`), logo o falso "used > max" existia la tambem. Commit isolado para revert trivial |
| P7 Modal logs de servico: sort + filtros por coluna | Portal | ✅ FEITO `1156168` — tokens CSS adaptados aos do V6 (`--color-line`, nao `--color-border`); strings PT hardcoded |
| Conhecer sem aplicar: falso-OK recalculateIssues; janela x3 de 31/07 (~09:30-17:00); AG SQLAGSPRD213 suspenso; DBA_DIFF_BACKUP QLT03 20:00 | Contexto | — |

**Validacao feita**: `py -3.14` ast nos ficheiros .py; 10/10 blocos de script
do portal sem erro de sintaxe (baseline V6 mantido) apos cada commit.

**Testes ao vivo (03/08, servico V6 reiniciado pelo owner)** — os primeiros
desta wave contra dados reais, e apanharam um fix que nao funcionava:

| Teste | Alvo | Resultado |
|---|---|---|
| Portal servido | `GET /watcherdb` | ✅ 9/9 anchors do lote presentes no HTML |
| 2.1 parse ISO | `SQLIDSPRD03_I01` (sem AG, 93 dbs) | ✅ **93/93** com `last_full` e `last_diff` preenchidos (antes: todas N/A) |
| 2.2 guarda (1a tentativa) | `SQLHDSPRD213_I01` | ❌ **500** / erro 976 na `MAP_SampleDB` — guarda `DATABASEPROPERTYEX` ineficaz |
| 2.2 guarda (pos-correccao) | `SQLHDSPRD213_I01` | ✅ **200**, 10 linhas = as 5 bases acessiveis; as 11 secundarias de AG excluidas |
| 2.2 regressao | `SQLIDSPRD03_I01` / `CAGENPRD06_I06` | ✅ 200 com 97 e 8 bases — a guarda nao exclui de mais |

⚠️ **ABERTO NO V3.3**: o `FILE_SPACE_DETAIL` do V3.3 (`queries.py:284`) usa o
MESMO predicado `DATABASEPROPERTYEX` que se provou ineficaz. O fix de 31/07
nunca foi testado contra o PRD213 (gate §5 item 4 por cumprir). Aplicar la a
guarda `NOT EXISTS`/`dm_hadr_database_replica_states` e re-testar.

**Pendente do owner (lote 31/07-tarde)**: browser test — modais Instances OK /
Backup Failed sem duplicados e com contagens iguais aos tiles; tile "Full
falhou" a 0 abre modal coerente (vazia limpa, nao "0 de 6"); painel Space sem
headers sobrepostos ao rolar. Decisao pendente: **P1 e P5** (features novas).

**Nota de ambiente**: o arranque do V6 demora mais de 25s — o `/healthz` recusa
ligacao nesse intervalo mesmo com o servico em `Running`. Esperar ~60s antes de
concluir que o restart falhou.
