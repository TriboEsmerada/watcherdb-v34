# SOLUCOES — indice de problemas ja resolvidos (por SINTOMA)

Criado 2026-08-19 (decisao owner). Uso: `/recall <sintoma>` (skill `.claude/skills/recall/`)
faz grep aqui PRIMEIRO. 1 linha por episodio, mais recente no topo, nunca apagar.
Formato: `| data | sintoma | causa-raiz | fix | ponteiro | palavras-chave |` (';' dentro das celulas; sem quebras).
Registo obrigatorio: `/wave-close` passo 6, `/postmortem` passo 7, FIND resolvido, ou qualquer fix que custou >30 min.
Nomes de servidores reais permitidos (doc interno; postmortems e' que sao anonimizados).
Back-fill inicial: extraido de CONTEXT.md (Diario 07-08/2026), findings-inbox.md, memoria e artifact de progresso — ponteiros verificados na sessao de 19/08; cobertura parcial (ver rodape).

| Data | Sintoma | Causa-raiz | Fix | Ponteiro | Palavras-chave |
|---|---|---|---|---|---|
| 2026-09-03 | Servico Running mas logs/service_stdout.log e service_stderr.log congelados (V3.3: ultima escrita 21/08 18:24; V34: nunca); `-Tail` mostra so' conteudo velho; stdout 18,9 MB sem .1 (rotacao nunca correu); unicos registos do arranque no Event Log Application | Com o binPath em python.exe (fix 31/08) o SCM entrega sys.stdout/sys.stderr VALIDOS para NUL; o redirect em WatcherDBService.__init__ so' testava `is None` (verdadeiro apenas com pythonservice.exe) -> nunca corria -> toda a saida da app descartada | helper `_std_needs_redirect()` = algum stream None OU `servicemanager.RunningAsService()`; __init__ usa-o; 3 testes puros. Portar V3.3 + V6. Diagnostico rapido: `Get-ChildItem logs\service_std*.log` LastWriteTime vs `Get-Service` Running | watcherdb_service.py `_std_needs_redirect`; SERVICO_V34_PASSO5_commit.ps1 | logs congelados service_stderr.log nao escreve stdout NUL python.exe host scm redirect is None RunningAsService rotacao nao roda event log |
| 2026-09-03 | Servico V34 recem-instalado (`watcherdb_service.py --startup auto install`) nao arranca: Start-Service falha de imediato, sem nada em service_stderr.log; `sc qc` mostra BINARY_PATH_NAME = `<venv>\pythonservice.exe` | REPETICAO do episodio 2026-08-31 (V33): host pythonservice.exe copiado para a raiz do venv e' incompleto (sem python311.dll ao lado, sem site-packages) — a AI nao correu /recall antes do passo D | Imediato: `sc.exe config binPath= '"<venv>\Scripts\python.exe" "<repo>\watcherdb_service.py"'`. Definitivo (commit PASSO 4): wrapper define `_exe_name_=sys.executable` + `_exe_args_="<wrapper>"` tambem em modo venv, logo install/update registam o python do venv directamente; `update` preserva LogOnAs | watcherdb_service.py classe WatcherDBService; SERVICO_V34_8434_RUNBOOK_2026-09-03.md passo 4 | pythonservice.exe venv servico nao arranca start-service binpath python.exe _exe_name_ _exe_args_ pywin32 install update handshake scm |
| 2026-09-03 | Script de commit .ps1 falha com dezenas de "error: pathspec 'X' did not match any file(s)" e o commit nao acontece (staging fica feito) | PowerShell 7 a passar `git commit -m $msg` com mensagem multi-linha contendo aspas duplas (`"Esperado"`): o PS parte a string em varios argumentos nas aspas e o git le o resto como pathspecs | Escrever a mensagem num ficheiro temp (`[IO.File]::WriteAllText`, UTF-8 sem BOM) e usar `git commit -F $file`; nunca `-m` com aspas duplas dentro; re-correr so' o commit (o `git rm`/`git add` ja' estavam staged — re-correr o script inteiro falharia no `git rm`) | docs/context/DELAYED_ROTULOS_PASSO1_commit.ps1 (versao corrigida); commit 7df2ffe | git commit -m aspas duplas pathspec did not match powershell pwsh argumentos split -F ficheiro mensagem multi-linha here-string |
| 2026-09-02 | Modal Diff Failed: paineis Total 21 contradizem tile 1 e header "1 em falta · 20 recuperadas"; owner: recuperadas na lista nao fazem sentido por defeito, painel tipo de barra unica e' ruido, "em falta" nao e' o termo (e' falha de job) | Fase 1/2 de 21/08 mantinha recuperadas na lista com selo MAS os paineis env/tipo somavam TODAS as linhas sem legenda (bars estaticas desde o render); terminologia "ainda em falta" ambigua com backup em atraso | Recuperadas ESCONDIDAS por omissao c/ chip mostrar/ocultar N (split contado ANTES do gate do toggle senao chip nunca sabe o N), barras env recontadas via _envBarCounts (cross-filter, respeita toggle), painel "Backups por Tipo" removido nas 2 modais, termo "NAO RECUPERADA" (chaves not_recovered*/show_recovered_toggle/hide_recovered_toggle {n} pt/en/es) | portal applyAllBackupFiltersWithMultiselect + toggleRecoveredRows; PROMPT_PROPAGACAO_V6_RECUPERADAS_TOGGLE_2026-09-02.md; branch wave-b-indexacao-dmv 02/09 | modal diff log failed total paineis contradiz tile recuperadas escondidas toggle mostrar ocultar nao recuperada ainda em falta not recovered barras ambiente estaticas tipo barra unica ruido numeros nao se contradizem |
| 2026-09-02 | Start/Restart de TODOS os servicos WatcherDB (V33, Collector, SSIS) falha com Event 7000 "account name is invalid or does not exist, or the password is invalid"; senha NAO mudou, conta activa; V6 continua Running | VPN GlobalProtect (adaptador "Ethernet 3" PANGP) caiu 15:56:33 (NetworkProfile 10001 tapnet.tap.pt) e so voltou 16:08:06; os 3 servicos estao registados com a conta em formato UPN (ue_e-snetto@tapnet.tap.pt) que exige DC/GC para resolver o logon, o V6 esta em formato DOMAIN\user (TAPNET\ue_e-snetto) e usa credencial em cache — sobreviveu | Reconectar VPN (perfil Domain Authenticated em Get-NetConnectionProfile) e Start-Service; para nao repetir, re-registar os 3 servicos em formato TAPNET\user (services.msc Log On, ou WMI Change com Get-Credential); correccao de fundo continua a ser conta de servico AD/gMSA (COMANDOS_UTEIS:80) | Event Log System 7000 15:59-16:07 + Microsoft-Windows-NetworkProfile/Operational 10001/10002; PLANO em .claude/plans (incidente 02/09 16:00) | servico nao arranca 7000 account name invalid password invalid vpn globalprotect pangp ethernet 3 upn domain user logon cache start-service restart-service collector v33 service start fails |
| 2026-09-02 | Sync do espelho standalone falhava: tar.exe "Damaged tar archive (bad header checksum)" em loop infinito de Retrying | Pipe do PowerShell (`git archive HEAD \| tar -x`) trata stream BINARIO como texto e re-codifica os bytes — o tar recebe o arquivo corrompido (bash-ism que nao sobrevive ao pipeline PS) | `git archive HEAD -o $tarFile -- WATCHERDB_V3.3` (escreve directo no ficheiro, sem pipe) + `tar -xf $tarFile -C $tmp` + robocopy /MIR /XD .git p/ a working copy + commit "chore: sync ... @ <sha>" + push origin main. Script permanente: docs/context/SYNC_STANDALONE_V33.ps1. REGRA: nunca pipar output binario entre executaveis no PowerShell — sempre via ficheiro | docs/context/SYNC_STANDALONE_V33.ps1; repo TriboEsmerada/watcherdb-v33 (snapshot sem historico, metodo 2026-07-08); CONTEXT.md 08/07 | tar damaged archive bad header checksum powershell pipe binario git archive snapshot standalone espelho watcherdb-v33 sync robocopy mir |
| 2026-09-02 | Restart-Service WatcherDBWebServiceV33 falhava a' PRIMEIRA (StartServiceFailed) e passava a' segunda — recorrente desde 23/07, atribuido a "mutex race, repetir" | Desenho do wrapper: o stop reporta STOPPED ao SCM ANTES de o processo morrer (janela graciosa ~30s do uvicorn) e o SO so' liberta o mutex single-instance na MORTE do processo; o arranque novo testava o mutex UMA vez e abortava ("instancia ja em execucao") | watcherdb_service.py: retry do acquire a cada 2s ate 45s a reportar SERVICE_START_PENDING c/ waitHint; log "mutex libertado pela instancia anterior" qd esperou; erro so apos deadline. Validado 02/09: restart passou a' primeira. Propagar a V6 (5o item boot hardening 23/07) | commit 2ea70ce; watcherdb_service.py SvcDoRun; Event Log 02/09 09:33 (abort) vs 09:55 (limpo) | restart service falha primeira segunda tentativa mutex single instance stopped scm janela graciosa start_pending retry arranque abortado |
| 2026-09-02 | Servico V33 fica em "Start Pending" (Get-Service) para sempre depois de um Restart-Service; a app SERVE na 8433 (healthz 200, python filho a escutar) mas Stop/Restart/Start sao recusados pelo SCM; sem Event 7000/7034, so' o 7039 habitual | Regressao do fix do mutex (linha acima): win32serviceutil.SvcRun reporta RUNNING ANTES de chamar SvcDoRun; o loop de espera pelo mutex voltava a reportar START_PENDING e ninguem repunha RUNNING — so' acontece quando o arranque teve de ESPERAR (restart rapido), por isso o start limpo das 09:55 nao apanhou | watcherdb_service.py: ReportServiceStatus(SERVICE_RUNNING) logo apos adquirir o mutex quando waited=True (+2 testes com stub do SCM em test_service_entry.py). Recuperar o estado preso: Stop-Process do python host+filho do servico (Win32_Service ProcessId; Get-NetTCPConnection 8433) -> SCM passa a Stopped -> Start-Service. Diagnostico: Get-CimInstance Win32_Service (State/ProcessId) + Get-NetTCPConnection -LocalPort 8433 | watcherdb_service.py SvcDoRun (waited); tests/unit/test_service_entry.py; incidente 02/09 19:19-19:50 | start pending preso stuck scm running nao reportado svcrun svcdorun mutex espera restart stop recusado 1061 cannot accept control healthz responde get-service startpending |
| 2026-09-02 | Recontagem das barras da modal escrevia o numero DENTRO da barra e a coluna da direita ficava com o valor VELHO (so' visivel qd unidades divergiram: 46 na barra, 106 a' direita) | row.querySelector('div:last-child') devolve o 1o DESCENDENTE que e' :last-child do SEU pai — apanhou o preenchimento da barra (filho unico do contentor), nao a coluna da direita (ultimo filho da ROW); bug invisivel enquanto recount e render davam o mesmo numero | usar row.lastElementChild p/ o numero + limpar textContent residual do preenchimento; REGRA: para "ultimo filho directo" e' lastElementChild ou :scope > :last-child, nunca querySelector(':last-child') | commit e97c050 (PASSO 4); portal applyAllBackupFiltersWithMultiselect | queryselector last-child descendente lastelementchild numero dentro da barra coluna velha recount chart modal |
| 2026-09-01 | Portal 8433 em timeout p/ terceiros (socio: "can't reach this page"); TODOS os testes no servidor passavam (nslookup OK, TcpTest True) | Adaptador da rede TAP (Ethernet 3, 10.89.0.171) DESLIGADO no portatil que serve o portal — o DNS resolvia para um IP que nao existia na rede; testes locais enganam porque a maquina responde a si propria (4 outputs seguidos vieram do proprio servidor em vez da maquina remota); colateral: Wi-Fi registava 192.168.1.184 no DNS do dominio (2o A record) | Enable-NetAdapter 'Ethernet 3' + confirmar IP 10.89.0.171 + ipconfig /registerdns; Wi-Fi com RegisterThisConnectionsAddress $false; REGRA: diagnostico de acesso remoto comeca com Get-NetAdapter e o teste remoto so' vale vindo de hostname != TI-PF5HQWK4 (ping falhado = rede, nao firewall); firewall/servico verificados saos (Allow 8433 Domain+Public 10.88.0.0/17+10.89.0.0/16) | memoria project_portal_tap_adapter_required; SOLUCOES 2026-06-12 (episodio irmao firewall profile) | portal inacessivel timeout cant reach 8433 adaptador tap ethernet vpn desligado dns registo duplo wifi 192.168 ping falha teste local engana hostname |
| 2026-08-31 | Suite pytest inteira abortava com INTERNALERROR (parecia problema de ambiente) e, depois de ressuscitada, 11 vermelhos cujos diagnosticos iniciais estavam 3/4 errados | Scripts manuais disfarcados de teste (test_*.py com codigo executavel no topo do modulo: sys.exit na coleccao = INTERNALERROR; requests top-level ao porto 7000 em cada coleccao); teste de mirroring com data LITERAL no mock vs filtro de 7 dias (expirou 24/08 — endpoint produzia mirror_errorlog); import module-level de constantes que sempre viveram DENTRO de SQLQueries; 1 vermelho era regressao real do PRODUTO (ladder hex hardcoded portal :42976 pos-F4) | 10 scripts renomeados check_*_manual.py (padrao favicon); mock com data relativa; produto tokenizado getSevTokens; guarda AST nova test_no_toplevel_execution_guard.py (apanhou 5 extra na 1a corrida); REGRA: vermelho de suite ressuscitada verifica-se NA FONTE antes de aceitar diagnostico | commits 56adaf4 4cd6214 + lote 31/08-01/09; PROMPT_V33_SUITE_TESTES_2026-08-31.md (revisto) | internalerror coleccao pytest suite abortava script disfarcado teste sys.exit requests top-level mock data literal expirou janela 7 dias mirror_errorlog ladder hardcoded getSevTokens guarda ast |
| 2026-08-31 | Modal Backup Delayed repetia a MESMA base 5x (TDH_MDS/SQLHDSQLT103); DB_AVAILABILITY 14 rows/7 dbs | NAO era view (a de DB_AVAILABILITY esta' certa, padrao TOP1 por env): WDB_INSTANCE_TCP_PORT com porta do VIZINHO em 2 entradas (SQLIJSQLT03 cached 55411=I01, real 50245; SQLHDSQLT105_I03 cached 62086=I07, real 50245 via SQL Browser UDP) -- 2 entradas do inventario ligavam a' mesma instancia real (dados em dobro) e as instancias verdadeiras ficaram SEM monitorizacao silenciosa; auto-heal nao dispara porque a porta errada LIGA com sucesso | UPDATE das 2 rows do port cache (owner) + FIND-20260831-102: assert de identidade pos-connect no collector (SERVERPROPERTY InstanceName vs esperado) | findings-inbox.md FIND-20260831-102 | modal repetido duplicado linhas dobro porta aprendida cache vizinho browser udp instancia errada gap silencioso auto-heal |
| 2026-08-31 | Modal mirroring mostrava o MESMO problema 3x (SUSPENDED triplicado); total da _ACTIVE 330 em vez de 110 | Migration da noite recriou a view a partir do canonical SECTION16, cujo CTE ActiveSlot ignorava a coluna Environment da KPI_STG_ACTIVE_TABLE (3 linhas por KPI, por desenho); CROSS JOIN multiplicava os branches; a view VIVA pre-31/08 estava certa -- canonical CONTAMINOU o vivo (drift FIND-20260810-101 ao contrario) | AND a.Environment='ENV' por branch; corrigido em triplo (BD viva via MIGRATION_MIRRORING_FIX_ENV_SLOT.sql + canonical SECTION16 + migration original); REGRA NOVA: antes de DROP/CREATE de view viva, diff sys.sql_modules vs canonical | CHANGELOG_JOBS_COLETA.md 2026-08-31 hotfix; CONTEXT.md 31/08 | mirroring triplicado duplicado modal view active slot environment cross join canonical drift contaminou vivo sql_modules |
| 2026-08-31 | Servico V33 nao arranca: 1053/7009 timeout SCM 30s em TODAS as tentativas (elevadas incl.); pythonservice.exe -debug morre silencioso; Event Log Application 'Python Service' com 'Cannot retrieve event message text' | pythonservice.exe COPIADO para a raiz do .venv-build pelo LocatePythonServiceExe do pywin32 (venv nao traz o exe em site-packages/win32) e' um host INCOMPLETO: (1) sem python311.dll/pywintypes311.dll ao lado -> processo morre no load antes do handshake SCM; (2) mesmo com DLLs copiadas, inicializa Python SEM site-packages do venv -> ModuleNotFoundError: servicemanager (texto so' visivel nas Properties CRUAS do evento — a mensagem formatada esconde-o) | binPath directo ao python do venv: sc.exe config binPath= '"<repo>\.venv-build\Scripts\python.exe" "<repo>\watcherdb_service.py"' — o main() do wrapper ja' hospeda o StartServiceCtrlDispatcher (desenho previsto; imports de topo leves, RUNNING antes do import pesado); sc config NAO mexe no ObjectName (LogOnAs preservado). REGRA: servico pywin32 em VENV regista-se com hosting directo pelo python.exe do venv, nunca pelo pythonservice.exe copiado; diagnostico de 'Cannot retrieve event message text' = ler $_.Properties do Get-WinEvent | v33_start_result + eventos SCM 7000/7009 31/08 09:19-09:35; watcherdb_service.py:389-407 (main dispatcher); SOLUCOES 2026-08-21 (episodio irmao: PythonClass orfao) | servico nao arranca 1053 7009 timeout scm pythonservice venv dll pywintypes servicemanager modulenotfound event log properties cannot retrieve message binpath python.exe hosting directo |
| 2026-08-21 | Servico V33 nao arranca apos restart (error code 1); depois de re-registado, browser ERR_SSL_PROTOCOL_ERROR no hostname | commit 5c74712 20/08 removeu services/web_service como 'codigo morto' verificando so imports+spec+docs; o registry SCM PythonClass apontava para la (dependencia invisivel ao grep); servico correu 24h com classe em RAM ate ao 1o restart. 2a perna: o wrapper novo le TLS de env vars (WATCHERDB_TLS_CERT/KEY) e o antigo lia do config.yaml removido (ssl.enabled desde 16/08, certs em C:\ProgramData\WatcherDB\certs INTACTOS) -> servico caiu de https p/ http e o browser (habituado a https) rejeitou | re-registo para o entry point novo: python watcherdb_service.py update (preserva LogOnAs) + Start-Service; owner acrescentou WATCHERDB_TLS_CERT/KEY ao .env + restart -> https de volta; REGRA NOVA: remover/mover modulo de servico exige verificar SCM (sc qc + PythonClass/ImagePath dos 4 servicos WatcherDB), PARIDADE DE CONTRATO (porta+esquema TLS+auth, nao so 'arranca e healthz responde') e restart de validacao na MESMA sessao; achado colateral: WatcherDBSSIS PythonClass aponta para pasta de BACKUP (C:\BKP PC TAP...) — mesma bomba armada | CONTEXT.md 2026-08-21; watcherdb_service.py:134-156; eventos Application 15:21-15:22 | servico nao arranca error code 1 pythonservice pythonclass registry scm modulo removido no module named service restart cleanup codigo morto web_service ssis backup ssl tls err_ssl_protocol_error https http esquema contrato |
| 2026-08-21 | KPI Full/Diff/Log falhou alarma 7 dias apos falha ja recuperada (76% do tile era ruido) e limparia por idade mesmo sem recuperar | falhas sysjobhistory tem Database NULL por desenho -> logica unresolved R+13 nunca dispara; saida do alarme era so envelhecimento na janela 7d do collector | Fase 1 (council 4 vozes, opcao E): recuperacao job-level via KPI_MSSQL_AGENT_JOBS_STG (helper partilhado card+modal, fail-open snapshot stale 15min, badge Recuperado no modal, tile conta so em-falta); Fase 2 pendente: Resolved_By_Success_TS no collector V1 + janela 30d | commit b13a7e2; api/routers/intelligence/helpers.py backup_job_failure_recovered; PLANO_JOBS_SCHEDULES_BACKUP_2026-08-17.md F5/P3 | backup full diff log falhou alarme 7 dias recuperado recovered database null sysjobhistory janela envelhecimento ruido falso negativo agent_jobs |
| 2026-08-21 | Wave declarada 100% fechada mas 5 itens do proprio plano por executar (apanhado pela sessao V6, nao por nos) | fecho declarado a partir da checklist mental das etapas, sem varrer as linhas 213-215 do plano nem grep final de leitores; sinal ignorado: sql_servers.json V3.3 nao arquivavel no E7 PORQUE tinha consumidores (boot SQLServerMonitoring 95 servers Excel, alwayson fallback, POST config, hardcoded) | E6c definida (item 0 da mini-wave); REGRA NOVA de fecho: wave so fecha com grep de consumidores remanescentes do legado = zero, e ficheiro legado arquivado E o arquivo a nao partir nada | PLANO_SERVERS_JSON bloco CORRECCAO 21/08; CONTEXT.md 2026-08-21; prompt V6 adenda 21/08 | fecho wave overclaim 100% grep leitores consumidores legado sql_servers.json e6c sombra checklist plano parecer v6 |
| 2026-08-20 | Servidor SQL 2005 com login OK mas zero availability desde sempre (parecia 'nao consegue logar') | sqlserver_start_time (2008+) no SELECT principal do inst_availability, FORA do version-gate -> batch falha 42S22 inteiro em 2005 | Uptime via tempdb create_date (portavel 2005+; delta 0h vs metodo antigo em 2016); gate ja cobria so a porta | scripts/collectors/collect_inst_availability.py QUERY; CONTEXT.md 2026-08-20 | sql 2005 sqlserver_start_time dm_os_sys_info 42s22 version gate tempdb create_date uptime availability oatxp01 suporte parcial versao |
| 2026-08-20 | Sidebar 62 vs Disponibilidade 61/Offline 0 (numeros nao batem; servidor configurado invisivel) | 2 servidores sem coleta: PRD214 porta dinamica mudou no reboot (porta aprendida stale + Browser bloqueado = circulo vicioso); OATXP01 SQL 2005 abaixo do piso 2014+ (CONNECTIONPROPERTY inexistente) | Fix A: retry via Browser qd porta aprendida falha (async_server_collector); OATXP01 enabled:false; B superficie no cartao aguarda owner | CONTEXT.md 2026-08-20; async_server_collector.py connect() | 62 61 offline zero porta stale dinamica reboot browser circulo vicioso sql 2005 connectionproperty piso versao invisivel coverage gap |
| 2026-08-20 | Servidor PRD (SQLHDSPRD502) coletado so' por agent_jobs/2PC; invisivel nos restantes KPIs | SQL 2008 R2 TLS falha c/ driver 17 -> enabled:false/removido do canonico; mas agent_jobs/2pc liam sql_servers.json (95, sem enabled) com driver legacy e continuavam | decisao owner na E5: aceitar perda ou re-entrar no canonico c/ driver legacy por servidor | CONTEXT.md 2026-08-20 E4c; findings-inbox.md:794 | sqlhdsprd502 sql 2008 tls driver legacy sql_servers.json agent_jobs 2pc gap coleta parcial |
| 2026-08-19 | collect_kpi QA nao seleccionava nenhum servidor de qualidade | env_mapping 'QA'->'qa' mas servers.json usa 'quality'; tasks collect_kpi_* estao enabled:false (codigo morto) | 'QA'->'quality' por higiene; em E5 mapa unico InventoryProvider.ENV_ALIASES | commit 030869e; V1 services/collector_service/collectors/collect_kpi.py:147-153; config.yaml:65-109 | collect_kpi env_mapping qa quality ambiente environment collector desactivado dead code |
| 2026-08-19 | Lista de servidores diverge entre V1/V3.3/V6/sql_servers.json/server_config/INST_ENVS (>=10 fontes) | Cada componente le o seu ficheiro/tabela; 4 copias de servers.json com cifras diferentes; sem loader unico | Plano servers.json fonte unica (E0-E8); E1 InventoryProvider + schema + 66 testes | docs/context/PLANO_SERVERS_JSON_FONTE_UNICA_2026-08-19.md; commits 2a8aa7e 030869e | servers.json inventario inventory monitored_servers server_config inst_envs duplicacao drift sql_servers.json |
| 2026-08-18 | Cartao 'DIFF >1D' em 32 bases mas API diz 0 issues (Backup OK no Overview) | Duas definicoes de issue: API suprime DIFF atrasado se ha FULL <24h; cliente recalculateIssues so limiar 12h; o FULL substitui o DIFF do dia | Cliente: min(hours_since_last_diff, hours_since_last_full) + limiar 26h; regra 'FULL perdoa o DIFF ate correr' | commit f15bfb8; templates/watcherdb_portal.html ~:20584; api/routers/backup_analysis.py:396-441 | backup diff full atraso overdue 26h recalculateIssues falso positivo backup ok |
| 2026-08-18 | Cartoes nao mudavam ao filtrar por ambiente (6 cartoes mostravam frota inteira) | evN caia no total quando faltava X_by_env (backend sem reparticao, nome diferente, ou frontend fixo) | +full/diff/other_failed_by_env, aliases down/unhealthy/critical_items_total; frontend ev(); teste de contrato test_kpi_env_breakdown_20260818 | commit b3459fd 17edba2; FIND-20260818-101; api/routers/intelligence_kpis.py | filtro ambiente environment by_env cartao kpi total frota env filter contract |
| 2026-08-18 | Botoes Collectors e Mutes invisiveis para TODOS os perfis (admin incluido) | commit 493c6de removeu data-dba-gated mas deixou style=display:none inline; segunda morte silenciosa (antes: CSP nonce 011e9dd) | remover display:none inline; smoke test de visibilidade do header por role | commit 7ba4da1; templates/watcherdb_portal.html:4053,4066; TRIAGEM_QA_RONDA2_2026-08-18.md | collectors mutes botao invisivel display none header role gate dba admin |
| 2026-08-18 | Endpoints admin devolvem 422 antes de 403 (corpo invalido valida antes da autorizacao) | _require_admin chamado inline no corpo; FastAPI resolve Depends antes do body; 8 endpoints em auth_compat.py | mover para `admin: dict = Depends(_require_admin)` na assinatura | api/routers/auth_compat.py:363,409,442,484,506,719,732,744; TRIAGEM_QA_RONDA2_2026-08-18.md | 422 403 depends require_admin auth_compat validacao autorizacao pydantic |
| 2026-08-17 | Aba Jobs a zeros ('Total Jobs: 0') em toda a frota ha 4 meses | response_model exigia server_id que o codigo nao enviava; handler de erro devolvia 200 com objecto vazio (264 ocorrencias desde 16/04) | response_model corrigido; handler devolve dados e loga o erro | commit c4437a0 | jobs zero total jobs response_model server_id 200 vazio silencioso agent jobs |
| 2026-08-17 | KPI 'Fila de CPU' (runnable/suspended) sempre 0 | collect_processes gravava estado da SESSAO (sleeping/running); vista contava estados do PEDIDO (runnable/suspended) que nunca chegavam | Status = COALESCE(estado do pedido, estado da sessao); rotulo 'Fila de CPU' | commit 7635f7c (V1 collect_processes) | fila cpu runnable suspended processes status coalesce sempre zero kpi morto |
| 2026-08-17 | Script de instalacao canonico congelado desde 22/04 — cliente novo herdaria BD sem colunas que o produto espera | INSTALACAO_COMPLETA_UNIFICADA.sql nao acompanhou waves (drift canonical) | Wave fresh-install repair (plano) + regra '5 surfaces' no wave-close | docs/context/PLANO_WAVE_FRESH_INSTALL_REPAIR_2026-08-05.md; PATCH_BLOCO_A_FRESH_INSTALL_BASE_TABLES_2026-08-06.sql | canonical instalacao fresh install drift ddl colunas em falta install script |
| 2026-08-13 | Primeira gravacao real de threshold dava 'Internal error' | tabela WDB_KPI_THRESHOLDS nao existia na BD viva (DDL de 04/08 nao sobreviveu; fallback de leitura escondeu a ausencia 9 dias) | owner recriou tabelas; canonical ganhou a seccao; erro 503 accionavel | commits 7055fe0 ea9ddfd ef9e5e0; CHANGELOG 2.x thresholds F1.5 | thresholds wdb_kpi_thresholds tabela ausente internal error 503 fallback esconde ddl nao aplicado |
| 2026-08-13 | Collectors/Mutes nao respondiam ao clique; ajuda do Performance muda; flash de tema | 3 blocos <script> sem nonce; CSP estrita (dev desde 12/08) bloqueava em silencio | nonce nos 3 blocos; V6 tem o mesmo padrao (handoff) | commit 011e9dd | csp nonce script bloqueado silencioso click nao responde portal |
| 2026-08-11 | Modal 'Instances OK' com filtro CASE LIKE por nome de instancia | padrao fragil; chave e' Instance em INST_ENVS | LEFT JOIN KPI_MSSQL_INST_ENVS (padrao directo) | FIND-102; api/routers/intelligence_kpis.py (batch B3 censo) | instances ok modal inst_envs like join ambiente env |
| 2026-08-07 | Painel pintava VERDE (Backup OK, Gaps 0, erros 0) por cima de leitura falhada | zero por defeito quando a query falhava; 'ausencia de dados' lida como saude; cartao de Servicos pintava VERMELHO em leitura falhada (cegueira lida como avaria) | contrato unico: verde so com leitura OK e resultado 0; leitura falhada = N/D cinzento | CONTEXT.md 2026-08-07; commits propagacao 8751f5b 1997848 dd9d232 (V6) | verde falso zero mentiroso n/d leitura falhada fail open backup ok gaps erros servicos |
| 2026-08-05 | Card Disponibilidade: 62 configuradas vs 50 Online (12 'falhas') | UDP 1434/SQL Browser bloqueado por firewall no workstation; instancias nomeadas com port:1433 placeholder no servers.json; sqlstate 08001 em 100% dos logs; intermitente | sproc usp_check_availability_coverage_gap (janela 3h); porta aprendida WDB_INSTANCE_TCP_PORT; pendente: AsyncServerCollector ler porta antes do Browser | CONTEXT.md 2026-08-05; V1 database/CHECK_AVAILABILITY_COVERAGE_GAP.sql; canonical SECAO 17B | disponibilidade availability 08001 sql browser udp 1434 porta 1433 placeholder named instance firewall coverage gap |
| 2026-08-05 | Rename SQLHDSQLT301_I01 -> SQLHDSSQLT301_I01 viveu 12 dias so em disco (drift nao detectado) | servers.json do V3.3 e' gitignored; 3 copias fisicas com cifras diferentes (V1 EncryptionManager vs V3.3 DPAPI) | D1 fechado: collect_kpi le a raiz; copia do servico renomeada .DEPRECATED_20260805; rename aplicado | commit 2dc6412; CONTEXT.md 2026-08-05 D1 | servers.json copias drift gitignored ss301 rename cifra dpapi fernet deprecated |
| 2026-08-05 | Indices com 0 leituras candidatos a drop ('par cego' IX_*_Instance/IX_*_UpdateTS) | usp_setup_environment_tables gerava 189 pares cegos em clones BLUE/GREEN x ambiente; 15M leituras c/ escrita inutil; CUIDADO: dm_db_index_usage_stats zera no restart | drop faseado pos-medicao (40d uptime); verificar sqlserver_start_time antes de agir sobre '0 leituras' | CONTEXT.md 2026-07-30/08-05; docs/context/EXECUTAR_DROP_INDICES_PAR_CEGO_2026-08-05.sql; INDICES_CHAVES_NATURAIS_STG_2026-08-05.md | indices par cego ix_instance ix_updatets usage stats restart drop redundante performance intelligence |
| 2026-08-04 | Poller 'Run now' do collector_service ligava a BD com Trusted_Connection e servidor hardcoded | service.py:519-527 nao importava ServerManager; violava Regra de Ouro #2 | master via ServerManager (sql_monitoring), porta/ip do servers.json | FIND-20260804-108; V1 services/collector_service/service.py:519-541 | trusted_connection hardcoded poller run now service.py regra de ouro identidade servermanager |
| 2026-08-04 | '100% de disponibilidade' era eco de coleta parada ha 50 min (ratchet) | disponibilidade calculada sem olhar a frescura dos dados | frescura real via Last_Swap_Time de KPI_STG_ACTIVE_TABLE; fim do ratchet | CONTEXT.md 2026-08-04 e 2026-08-18 (lotes frescura); commit d64ce2a | disponibilidade 100 ratchet frescura last_swap_time kpi_stg_active_table freshness stale |
| 2026-08-03 | TempDB 'Saude do Disco' sempre a zero | KPI lia coluna Usage_Percent que nao existe na vista (nome real Percent_Used): linhas ignoradas em silencio | coluna corrigida no painel e na modal | CONTEXT.md 2026-08-03 | tempdb usage_percent percent_used coluna inexistente zero silencioso saude disco |
| 2026-08-03 | Mirroring a 0 com frota espelhada (vista partida desde 31/07) | eco da Wave C: coluna que deixou de existir na MIRRORING_DET_VIEW | CREATE OR ALTER da view (110 DBs SYNCHRONIZED validadas); FIND-105 | CONTEXT.md 2026-08-03; canonical (seccao mirroring) | mirroring view partida wave c coluna zero falso saude espelhamento find-105 |
| 2026-08-03 | Latencia de disco estruturalmente quebrada (medicao errada na fonte) | collector media mal (confirmado por gate do especialista) | corrigido na fonte no mesmo dia + validacao em producao | CONTEXT.md 2026-08-03 | latencia disco disk latency collector medicao fonte |
| 2026-07-31 | Varias vistas _ACTIVE devolviam 3x as linhas (fan-out) | UNION das BLUE/GREEN sem filtro por ambiente apos particionar KPI_STG_ACTIVE_TABLE por Environment (Wave C) | uma bandeira por ambiente nas vistas; 37-38 combinacoes -> 0 | CONTEXT.md 2026-07-31 (Wave C validada) | fan-out x3 active view blue green environment wave c kpi_stg_active_table duplicados |
| 2026-07-30 | 26 bases de PRD sem backup ha >1 mes; painel dizia '0 FULL falhados' | job de backup activo mas AGENDAMENTO desligado a 21/06 — nunca corre, logo nunca falha | re-ligar agendamento apos perceber o motivo; job passa a registar erro | CONTEXT.md 2026-07-30; artifact progresso 'achado maior' | backup prd sem backup agendamento schedule desligado job nao corre silencio 0 falhados |
| 2026-07-30 | Hipotese 'heaps/forwarded records degradam a Intelligence' | FALSA: 0 forwarded records (TRUNCATE+INSERT desaloca; sem UPDATE); custo real eram os indices par cego | alvo mudou para os indices; nao mexer em heaps | CONTEXT.md 2026-07-30 | heaps forwarded records performance intelligence hipotese refutada truncate insert |
| 2026-07-28 | R7 'caminho de ligacao unico' dado como satisfeito — monitoring.py classificado 'codigo morto' | modules/monitoring/monitoring.py::ConnectionInfo E' o caminho do modulo SPACE; classificacao de outra sessao estava errada | ConnectionInfo delega no connection_pool (server_target/server_spn) | commit aa686d5; CONTEXT.md 2026-07-28 | monitoring.py connectioninfo space connection_pool codigo morto verificar na fonte licao |
| 2026-07-28 | Carregamento do portal mostrava 'so os segundos' / passos do checklist sumiam | carregamentos sobrepostos colidiam no checklist | causa-raiz corrigida (sequenciacao) | CONTEXT.md 2026-07-28; artifact progresso 3 | checklist carregamento loading sobreposto portal |
| 2026-07-27 | 4 tasks NEVER_RAN no Collector Health (falsos alarmes) | (A) collect_storage_* com metadata errada no config.yaml (modulo inexistente, tabela nunca existiu); (B) reconcile declarava 1440 vs tick 60 sem heartbeat | config.yaml corrigido; heartbeat no adapter + seed SCHEDULE_META HEARTBEAT + fallback no health_calculator | CONTEXT.md 2026-07-27 (OODA NEVER_RAN, FIX B) | never_ran collector health heartbeat schedule_meta config.yaml collect_storage falso alarme |
| 2026-07-27 | KPI de espaco acusava dezenas de 'criticos' em ficheiros saudaveis | ficheiros em autogrowth sem teto contados como cheios | modelo por tipo de crescimento (com teto: livre vs teto; sem teto: disco); 'verificar no host' para discos sem visibilidade | CONTEXT.md 2026-07-27; artifact progresso 2 | espaco filegroup autogrowth critico falso positivo livre efetivo unlimited disco |
| 2026-07-25 | 85 rows de INST_ENVS com Env fora da convencao; dashboards 'Por Ambiente' perderam PRD/TST | sync_servers_from_json mapeava PROD/TEST (bug latente) — activado quando o sync passou a diario | mapping PRD/QLT/TST nos 2 CASEs + usp_reconcile_inst_envs + UPDATE de dados (hotfix 2.25.1) | commit 2ddde3a; V1 scripts/sync_servers_from_json.py:211-247; canonical SECAO 17 | inst_envs env prod test prd tst convencao ambiente mapping sync hotfix 2.25.1 por ambiente |
| 2026-07-24 | Servidor permanentemente '1 OFF' na disponibilidade | SQLHDSTST014_I03 enabled:false num servers.json e sem a chave noutro; ServerManager default enabled=True | enabled explicito na copia do servico; depois: unificacao das copias + enabled obrigatorio (Q8 2026-08-19) | DESIGN_RECONCILE_INST_ENVS_2026-07-23.md; V1 server_manager.py:156 | 1 off enabled default true servers.json copias disponibilidade server_manager |
| 2026-07-24 | Instancias fantasma (orfas) no portal — INST_ENVS append-only com 101 rows | tabela nunca reconciliada desde migracao 25/03; 33 servers disabled | usp_reconcile_inst_envs (UPDATE/INSERT/DELETE com guarda 30d, DryRun default) + adapter diario | DESIGN_RECONCILE_INST_ENVS_2026-07-23.md; commit 2ddde3a | inst_envs orfaos fantasma reconcile append-only purge grace 30d instancias removidas |
| 2026-07-23 | Hangs de minutos a ligar a SQLHDSTST505 (DNS flap) | DNS local resolvia com atraso/oscilacao; porta I01 dinamica 50760 | entrada em hosts 172.17.152.49; porta estatica pedida a rede; net cache + port discovery (Wave A) | memoria project_dns_flap_intelligence_hosts_fix; docs/context/PEDIDO_PORTA_ESTATICA_I01_2026-08-04.md; DESIGN_RESILIENCIA_REDE_2026-07-23.md | dns flap hosts sqlhdstst505 porta dinamica 50760 hang ligacao wdb_host_ip_cache wdb_instance_tcp_port |
| 2026-07-23 | Dashboard a servir dados de Maio como se fossem de hoje | 4 familias de collectors paradas desde 13/05 (migracao 25/03 partiu chaves backslash vs underscore; config.yaml NAO em git -> regressao silenciosa) | sweep + religar collectors; config.yaml para git/backup; frescura visivel | memoria project_collector_freshness_findings_2026_07; CONTEXT.md 2026-07-17 (parecer v1-intel) | collectors parados frescura stale dados de maio config.yaml git regressao backslash underscore chaves |
| 2026-07-23 | Servico Windows (pywin32+uvicorn) nao parava/arrancava limpo; threads presas | faltava should_exit + daemon=True; DB sincrona dentro de async; sem /healthz | padrao: should_exit+daemon, to_thread para DB, /healthz, monitor 5 camadas (4 defeitos identicos V3.3+V6) | memoria feedback_windows_service_boot_hardening; watcherdb_service.py | windows service pywin32 uvicorn should_exit daemon healthz boot hardening thread presa |
| 2026-07-17 | Mirroring e errorlog so coletados em PRD (QA/TST sem dados) | CHANGELOG V1 2.17.0 (14/04) ja corrigira; migracao 13/05 reverteu porque config.yaml nao esta em git | re-aplicar tasks QA/TST no config.yaml; risco documentado | CONTEXT.md 2026-07-17 | mirroring errorlog qa tst config.yaml regressao repetida git |
| 2026-06-12 | Portal inacessivel de outra maquina apesar da regra de firewall | regra com -Profile Domain mas a interface estava no perfil Public | New-NetFirewallRule -Profile Domain,Public -RemoteAddress <subnet> | memoria feedback_firewall_profile_interface | firewall profile domain public interface porta 8433 inacessivel |
| 2026-06-01 | Commits PowerShell falhavam ('unknown switch', pathspec errors) | single-quoted -m com `"`, `->`, `[[..]]` quebra o parsing de argv | multiplos -m, caracteres seguros; heredoc @'..'@ parte com quotes interiores | memoria feedback_powershell_commit_msg_safe_chars; feedback_powershell_heredoc_vs_multi_m_commits | powershell git commit mensagem caracteres unknown switch pathspec heredoc |
| 2026-06-01 | Click handler de ordenacao nao disparava (Wave S) | JSON.stringify em onclick double-quoted no HTML partia o atributo | browser test obrigatorio pre-ship para handlers inline | memoria feedback_browser_test_event_handlers_pre_ship | onclick inline json stringify atributo html handler nao dispara browser test |
| 2026-05-27 | Edits de 24h+ por commitar sem ninguem reparar | WATCHERDB_V6 tem git proprio local-only; `git status` no parent nao o mostra | git status por repo em bundles cross-repo | memoria feedback_multi_repo_commit_verification; commit 98c18ed | git status multi repo v6 uncommitted bundle cross-repo |
| 2026-05-26 | ReferenceErrors em cascata no portal mascarando o bug real | apostrofe unescaped numa string single-quoted JS -> SyntaxError derruba o script block inteiro | hotfix e3c734b; regra: nunca apostrofe sem escape em single-quoted | memoria feedback_js_string_quoting; commit e3c734b | javascript apostrofe syntaxerror referenceerror script block portal |
| 2026-05-23 | Collectors que ligam directo a servidores monitorizados falhavam a decifrar password | ServerManager prometia decifrar monitored_servers mas nunca o fazia; 7 collectors com decrypt inline | ServerManager decifra uma vez (Wave R+5); inline decrypts ficam idempotentes | memoria feedback_server_manager_decrypt_gap; V1 server_manager.py:125-144 | servermanager decrypt password fernet encrypted monitored servers wave r+5 |
| 2026-05-07 | ~33 instancias sem KPI silenciosamente (gap de coleta) | driver ODBC legacy 'SQL Server' + SQL 2019 FCI + porta dinamica = ligacao falha sem erro util | driver moderno + correccao em sync E async collectors (paridade) | FIND-20260507-001; memoria project_v1_driver_dynamic_ports; feedback_collector_parity | driver odbc legacy sql server porta dinamica fci kpi gap silencioso sync async paridade |
| 2026-05-05 | Card 'SQL Nao Respondeu' mostrava instancia mas clicar dava 'Servidor nao encontrado' | SQLHDSQLT301_I01/_I02 em server_config (BD) mas ausentes do servers.json V3.3 (duas fontes sem sync) | entradas adicionadas ao JSON; causa estrutural -> plano fonte unica 2026-08-19 | findings-inbox.md:598-610 (FIND-20260505-103); FIND-104 discovery | servidor nao encontrado server_config servers.json sync sqlhdsqlt301 duas fontes drill-down |
| 2026-04-29 | Leitores de servers.json partiam quando o servico corre como Windows Service (CWD=System32) | Path('config/servers.json') relativo em 8 leitores | resolver absoluto (watcherdb_main.py:110 config_dir 3-tier); restantes pendentes no plano fonte unica | FIND-20260429-002 (findings-inbox.md:410-415) | path relativo cwd system32 windows service servers.json config_dir |

Por cobrir no back-fill (fazer na proxima `/manutencao-contexto`): entradas de CONTEXT.md anteriores a 2026-07-17; findings-inbox FIND-101..111 com status resolved que nao estejam acima; docs/bugfixes/; CHANGELOG V1 (fixes 2.x); postmortems formais (anonimizados) se existirem.
