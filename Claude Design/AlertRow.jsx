**StatCard** — see KpiCard.prompt.md (same directory). Compact label+value cell for dense stat strips; left rail carries severity.

```jsx
<StatCard label="Memory" value="248" unit="GB" state="info" />
```
